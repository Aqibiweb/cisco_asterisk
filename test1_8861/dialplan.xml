<?xml version="1.0" encoding="UTF-8"?>
<dialTemplate>
  <versionStamp>83f69fbc-0525-46be-b492-73d806a0299b</versionStamp>
  
  <!-- Emergency Numbers -->
  <TEMPLATE match="911" timeout="0" />
  <TEMPLATE match="999" timeout="0" />
  <TEMPLATE match="112" timeout="0" />
  
  <!-- Feature Codes - Immediate dial -->
  <!-- Call Forward All - activate -->
  <TEMPLATE match="*72." timeout="3" />
  <!-- Call Forward All - deactivate -->
  <TEMPLATE match="*73" timeout="0" />
  <!-- Call Forward Busy - activate -->
  <TEMPLATE match="*90." timeout="3" />
  <!-- Call Forward Busy - deactivate -->
  <TEMPLATE match="*91" timeout="0" />
  <!-- Call Forward No Answer - activate -->
  <TEMPLATE match="*92." timeout="3" />
  <!-- Call Forward No Answer - deactivate -->
  <TEMPLATE match="*93" timeout="0" />
  
  <!-- Call Park -->
  <TEMPLATE match="*70" timeout="0" />
  <!-- Call Park Retrieve -->
  <TEMPLATE match="*71." timeout="3" />
  
  <!-- Call Pickup -->
  <TEMPLATE match="*8" timeout="0" />
  <!-- Directed Call Pickup -->
  <TEMPLATE match="*88." timeout="3" />
  <!-- Group Call Pickup -->
  <TEMPLATE match="*98" timeout="0" />
  
  <!-- Do Not Disturb -->
  <TEMPLATE match="*78" timeout="0" />
  <TEMPLATE match="*79" timeout="0" />
  
  <!-- Voicemail -->
  <TEMPLATE match="*97" timeout="0" />
  <TEMPLATE match="*98" timeout="0" />
  
  <!-- Blind Transfer prefix (if your PBX uses it) -->
  <TEMPLATE match="*2." timeout="3" />
  
  <!-- Conference bridge (if your PBX has one) -->
  <TEMPLATE match="*9." timeout="3" />
  
  <!-- Intercom -->
  <TEMPLATE match="*80." timeout="3" />
  
  <!-- Redial -->
  <TEMPLATE match="*07" timeout="0" />
  
  <!-- Call Return (last caller) -->
  <TEMPLATE match="*69" timeout="0" />
  
  <!-- 3-digit extensions (100-999) -->
  <TEMPLATE match="[1-9]XX" timeout="3" />
  
  <!-- 4-digit extensions (1000-9999) -->
  <TEMPLATE match="[1-9]XXX" timeout="3" />
  
  <!-- 5-digit extensions (10000-99999) -->
  <TEMPLATE match="[1-9]XXXX" timeout="4" />
  
  <!-- External calls starting with 0 (local/national) -->
  <TEMPLATE match="0." timeout="5" />
  
  <!-- External calls starting with 00 (international) -->
  <TEMPLATE match="00." timeout="5" />
  
  <!-- External calls starting with 9 (outside line) -->
  <TEMPLATE match="9,." timeout="5" />
  
  <!-- 7-digit local numbers -->
  <TEMPLATE match="[2-9]XXXXXX" timeout="4" />
  
  <!-- 10-digit numbers -->
  <TEMPLATE match="[2-9]XXXXXXXXX" timeout="4" />
  
  <!-- Star codes general pattern -->
  <TEMPLATE match="*." timeout="5" />
  
  <!-- Pound codes general pattern -->
  <TEMPLATE match="#." timeout="5" />
  
  <!-- Catch-all with longer timeout -->
  <TEMPLATE match="." timeout="10" />
  
</dialTemplate>
